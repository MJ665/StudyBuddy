{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "venv/bin/uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000",
    "lint": "echo 'Python linting: use ruff or flake8'",
    "test": "echo 'No tests configured yet'"
  }
}

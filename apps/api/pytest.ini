[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
markers =
    live: tests that hit real external services (Neon Postgres, Neo4j Aura, Gemini). Skipped without creds/network.
filterwarnings =
    ignore::DeprecationWarning

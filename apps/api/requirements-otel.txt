# OpenTelemetry — OPTIONAL. Install ONLY when switching TELEMETRY_BACKEND=otel:
#     pip install -r requirements-otel.txt
# Keeping these out of the default image keeps it lean; observability/telemetry.py
# lazy-imports them, so the Sentry-only deployment never needs them.
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-sqlalchemy>=0.48b0
opentelemetry-instrumentation-httpx>=0.48b0
opentelemetry-instrumentation-logging>=0.48b0
